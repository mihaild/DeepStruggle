# One night's measurements, 2026-09-18 — and nine things that turned out to be wrong

An index for the session, in the order the results arrived, with each claim at the confidence it
actually earned. Every number here is recorded in full somewhere else; nothing in this file is the
only copy.

## What was established

| # | finding | strength | where |
|---:|:---|:---|:---|
| 1 | **The X4b collapse was mostly opponent-pool starvation.** `--resume <dir>` rebuilt the pool from the *parent* of the run directory, found no snapshots, and fell through to a pool of **one** — beaten 99.7%. Same resume state, same seed, same step: **24.0%** starved against **90.0%** with a working pool | **strong.** The only behavioural code change between the two runs is the fix itself; the other four commits are metrics-only or default-off | [`P15_X4b_collapse_is_pool_starvation.md`](P15_X4b_collapse_is_pool_starvation.md) |
| 2 | **The rollout bands reliably change the ending mix.** `0.8 1.2 0.7 1.1` against the default `0.15 0.50 0.10 0.35` halves DEFCON-1 endings, cuts the self-inflicted share by half to two thirds, and adds a full turn to the average game — **on both seeds** | **strong.** Replicated | [`P15_rollout_temperature.md`](P15_rollout_temperature.md) |
| 2b | **The strength gain replicates in direction, at disputed magnitude.** Both seeds beat the control at every point from 40M to 60M — seed A by 2.0–3.4x, seed B by 1.6–2.3x — and seed B lands inside the pre-registered 15–28% confirm band at 50M (16.5%) and 60M (22.5%), significant at both. The *size* still disagrees by 1.4–1.7x, and there is only **one control seed** | **moderate.** Two treatment seeds, one control seed | same |
| 2c | **The ending mix is not why the strong seed is strong.** Seed B has the *better* ending mix — DEFCON-1 29.2% against seed A's 32.6%, self-inflicted 6.25% against 12.6% — and the smaller gain. So bands → fewer nuclear endings is solid; fewer nuclear endings → stronger policy is not | **strong dissociation** | same |
| 3 | **The strength is general, not an anchor exploit.** At 70M the arm rates 1438.5 and beats `n26_280M` 80.0% and `p29_280M` 87.5% — 280M checkpoints from lineages it never saw | **strong.** Three independent lineages | same |
| 4 | **Opening quality and playing strength are separable.** That arm never places in West Germany (0.0/4), dumps 5.1/1 into Yugoslavia, sits at half the anchor's reference overlap — and is the second-strongest model in a four-way field | **strong**, and it falsifies using opening overlap as a proxy for strength | same |
| 5 | **Search targets contained illegal actions.** Determinized search legitimately proposes actions illegal in the real state; `batched_mcts.py` handles this for an acting agent and the training-target path never did. Visible as `search_ce` above 1e5 — impossible for a 212-way softmax — on a third of iterations of every search arm | **strong**, arithmetic checks out to a small factor. Fixed, tested, verified bit-identical | [`P15_kl_domination.md`](P15_kl_domination.md) §appendix |
| 6 | **`policy_loss` never logged the policy loss.** It accumulates the PPO surrogate alone; the KL, entropy and CE terms are all differentiated and none reach the log. So the objective actually minimised has never been recorded on any run | **strong**, it is a code fact. Fixed by adding keys | same |
| 7 | **Target degradation is a poor fit for the seat difference, but no longer excluded.** Both seats' targets flatten by the same proportion (US +15.9%, USSR +16.0%) and both policies drift from them equally (+14.6%, +14.5%), while the win rate falls **−17.3 pp on US and −29.6 pp on USSR**, a 1.71x difference. Equal cause, unequal effect | **weak.** Was reported as a clean exclusion on the strength of a retracted "USSR-only" claim | [`P15_kl_domination.md`](P15_kl_domination.md) |
| 8 | **The targets flatten *after* the policy does.** Win rate falls at 30.02M; targets flatten at 30.80M. The searcher is guided by the same network, so flatter targets are downstream of the decline. A loop may amplify; it cannot have started it | **moderate** — an ordering on 250k-spaced snapshots | same |
| 9 | **USSR search targets carry a standing 14–16% diffuseness handicap**, constant before and during the decline, and it is **not** determinization: the ratio is 1.13–1.27 with perfect information too, slightly larger. A fact about the game's positions, not about the searcher | **strong**, and independent of every retraction below | same |

## What was wrong, and corrected in the same session

This is the more useful list.

1. **"The collapse was an artifact."** Too strong. `E3-35-28` declines at ~30M with a
   demonstrably healthy pool — 73.6% → 58.2% — carrying the documented signature. Starvation
   **accelerated and deepened** a real failure mode rather than inventing it. (An earlier
   version of this item also said the healthy-pool decline plateaus at ~58%; see item 8.)
2. **"The KL regulariser crowds the CE term out of the update."** Refuted by the same arm.
   Iterations with `kl_div` > 1.0 average `grad_frac` 0.1166 against 0.1339 when it is low — no
   effect, wrong direction — and `grad_frac` fell to its lowest value *after* the KL returned to
   below its original baseline.
3. **"The treatment at 70M exceeds the control's best over 240M."** Not established. The same
   pair reads 33.3% on a second sample against 44.5% on the first; these percentages carry
   **±4–5 pp** and a 6-point gap does not survive that.
4. **Two dense arms ran concurrently for ~90 minutes**, byte-identical, each holding 8.7 GB and
   halving the other's throughput, because a launch script ran twice with nothing to notice.
5. **"The ending mix is why the treatment is stronger."** Both replicate, but they do not track:
   seed B reduces DEFCON-1 endings *more* than seed A (29.2% against 32.6%, self-inflicted 6.25%
   against 12.6%) and gains *less* at every matched point. The ending mix is **what the bands
   do**, reliably; it is not what separates a strong seed from a weaker one.
6. **"The temperature arm buys the seat the control never learns."** The seat asymmetry flipped
   between seeds — A was US-leaning (21.0 / 10.0), B is USSR-leaning (5.0 / 13.0) — so it is a
   property of a *seed*, not of the treatment.

7. **"The CE feedback loop is dead."** Retracted. That rested on target entropy being flat,
   measured over a window that ended ~0.5M steps before the flattening began. Extended to 32.3M
   the targets flatten **+18%**. The loop is still not the *initiator* — the ordering rules that
   out — but it was wrong to call it dead, and the error was a window boundary, not the analysis.

8. **"The healthy-pool decline plateaus at ~58%."** It held 54–60% for ~1.5M steps and then
   resumed falling; it is at **43.0%** by 33.03M.
9. **"The decline is USSR-only; the US seat does not move."** Window means over 21 points:
   **US −17.3 pp** (80.7 → 63.4) and **USSR −29.6 pp** (62.4 → 32.9). Both seats decline, USSR
   1.71x as much. The claim compared the US column's first and last values inside a window whose
   last point was 82 — one of its highest samples — next to an early 84.

Each of these was caught by running the check that could falsify the claim rather than the one
that would confirm it. The pattern is worth more than any single item on the list above.

**And three of the nine — items 7, 8 and 9 — are the same error**: a trend read off the endpoints
of a noisy series, or a null read off a window that had not finished. These evaluation points
carry **±4–5 pp**, so two of them carry nothing. Any claim about a direction here needs a window
mean and a stated n. Every one of those three had a sound instrument and a lazy reading, which
makes it the most transferable thing in this file.

## The open question, sharper than it was

**Why does the USSR seat fall almost twice as far as the US seat?** Both decline; the question is the 1.71x. It is
sharp because **four** candidate answers have been measured and excluded:

* *not the opponent pool* — capacity 12, span 30M, opponent win rate 0.45–0.49 throughout;
* *poorly explained by target quality* — both seats' targets degrade by the same ~16% while the win-rate loss differs 1.71x. A quantitative mismatch, not an exclusion;
* *not the KL* — `grad_frac` is unrelated to `kl_div` and kept falling after the KL normalised;
* *not determinization* — the USSR/US target-diffuseness ratio is 1.13–1.27 **with perfect
  information too**, slightly larger than with determinization, so hidden-state sampling does not
  create it.

**No proposed mechanism explains it well.** Target quality is the least bad of them and fits badly. That is a weaker position than this file claimed an hour earlier, when a retracted "USSR-only" reading appeared to exclude it outright.
What survives is a *fact about the game rather than about training*: at USSR decisions the search
genuinely finds more moves of near-equal value. That is constant across the whole window, so it
cannot be what takes the seat from 71% to 26% — at most it says which seat sits closer to the
edge.

A second, narrower question rides along: **why does `search_ce_grad_frac` fall 0.238 → 0.025** when
the policy is drifting *further* from its targets, which should make (π − p_target) larger? The
candidates are the other terms' gradients growing or the searched-row count falling, and both are
now instrumented (`search_ce_grad_norm`, `total_grad_norm`, `search_ce_rows`), so the next search
arm answers it rather than another argument.

## Left for the owner

* **The KL covers the opponent's decision rows** while the surrogate and entropy bonus are both
  restricted to `b_learner`. The comment beside `own_entropy` gives the argument for why that is
  wrong for entropy and it applies unchanged to a KL penalty. Changing it changes the objective.
* **Changing the default rollout bands** now has two-seed support and one missing arm. Both treatment seeds beat the control at every point from 40M to 60M, and seed B entered the pre-registered confirm band at 50M and 60M. What is still absent is a **matched control at a second seed**: with one control seed, nothing measured separates the treatment from between-seed variation in the control. That arm, not more points on these two, is what would settle the default.

## Tooling added

`tools/scripts/resume_args.py` (reconstruct a run's argv from its own metadata, because a
hyperparameter mismatch on resume fails silently where an architecture mismatch fails loudly),
`tools/scripts/search_target_entropy.py` (the offline probe behind item 7), the per-decision-type
table `collapse_trace.py` computed and then discarded, a loud warning when a resumed pooled run
lands on a pool of one, and the importance-ratio, gradient-norm and target-alignment diagnostics.
